import RestPassword from "../component/Authentification/restpassword";
import Layout from "../component/Authentification/layout";
export default function PageResetPassword() {
  return (
      <Layout>
        <RestPassword />
      </Layout>
    
  );
}
